# v1 — the layer as `init` first scaffolded it

**What changed:** Nothing to convert. This is the shape `/intent:init` wrote before any
migration existed: an index with the four rules and the attribution legend, an alignment register
keyed by row number, topic documents as dictated.

**Preconditions:** none.

**Apply (→ v1):** Nothing to apply. This file exists so the floor has something to point at, and
so the history records what a `1` in the bus means.

The one thing a v1 project is missing is its **`## Commands` migrate cell**, written as
`n/a — no migrations yet` before this skill shipped. The migrate procedure repoints it when it
re-stamps (step 5), and `/intent:init` does the same on a re-scaffold; `/intent:doctor` reports
the disagreement until one of them runs.
