---
name: init
description: >-
  Scaffold a repo's project memory — the `domains/` knowledge system: per-topic files of proven
  workflows, conventions and gotchas, plus the `INDEX.md` manifest that maps a task's topics to
  the files worth loading, a maintenance guide, and a re-runnable project scan. Registers itself
  in the config bus so other tools can find what applies without reading this plugin. Use
  whenever the user says "domains init", "set up project memory", "set up domains", "scaffold
  the knowledge system", or asks to capture what a codebase knows about itself. Safe to re-run —
  it fills gaps and leaves everything else alone. Delivering a ticket is a different tool's job.
user-invokable: true
---

Read `../_shared/init.md` and follow it. Read `../_shared/templates.md` for the file skeletons
and `../_shared/environments.md` for topic seeding hints.
