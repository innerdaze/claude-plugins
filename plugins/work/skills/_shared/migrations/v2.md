# v2 — memory + proactive-domain Rules bullets

**What changed:** The CLAUDE.md **Rules** section gained two bullets — one telling the
assistant to keep its auto-memory current, one telling it to load domains proactively.

**Apply (v1 → v2):** In the project's `CLAUDE.md` (or wherever its `## Rules` section lives
— follow `@`-imports), find the `## Rules` heading and **insert each of the following
bullets only if a bullet with the same intent is not already present** (match by intent,
not verbatim — skip one the project already states in its own words). Add nothing else;
touch no other bullet.

```markdown
- **Keep memory current** — proactively update your auto memory when you learn something worth remembering for future sessions (user preferences, project decisions, useful feedback, gotchas encountered). Don't wait to be asked.
- **Proactively load domains** — run `prep <domain>` for relevant domains without being asked; use the user's prompt for initial discovery, then load more domains as the work reveals new topics.
```

If the project has no `## Rules` section at all (very old or hand-stripped), report that
and ask before creating one — don't silently add a heading the user removed.
