# v1 — baseline

**What changed:** Initial scaffolding format. This is the version every project
initialised before the versioning mechanism already has, so a **missing** stamp is treated
as v1 and needs no migration.

**Apply (→ v1):** none. Baseline.
