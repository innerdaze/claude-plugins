---
description: Break a roadmap milestone into epics and tickets in the project's tracker, respecting the flow's decision rights — creating the work directly under a solo flow, or producing a planning pack for the team to commit under a sprint flow.
argument-hint: "[milestone name or id — defaults to the current one]"
---

Break down a milestone by following the `cadence-plan` skill at
`${CLAUDE_PLUGIN_ROOT}/skills/cadence-plan/SKILL.md`. Read that skill and follow it exactly.

If an argument is given, it names the milestone to plan; otherwise use the current milestone from the
roadmap.

$ARGUMENTS
